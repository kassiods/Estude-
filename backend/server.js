// Este servidor backend Express foi desativado.
// Para remover completamente o backend, por favor, delete o diretório 'backend/' do seu projeto.

console.log("O servidor backend Express está desativado e o diretório 'backend/' deve ser removido.");
console.log("Nenhum servidor será iniciado a partir deste arquivo.");

// Você pode adicionar um process.exit(0); se quiser que o script termine imediatamente ao ser executado.
// process.exit(0);
